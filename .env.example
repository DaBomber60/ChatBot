# HomeChatBot Environment Configuration
# Copy this file to .env.local and modify the values as needed

# Application
NODE_ENV=development
APP_PORT=3000
JWT_SECRET=your-super-secure-jwt-secret-key-change-this

# Database Configuration (PostgreSQL)
# For development, you can use Docker Compose PostgreSQL:
DATABASE_URL="postgresql://homechatbot:homechatbot_secure_password@localhost:5432/homechatbot?schema=public"

# PostgreSQL Docker Configuration (used by docker-compose.yml)
POSTGRES_DB=homechatbot
POSTGRES_USER=homechatbot
POSTGRES_PASSWORD=homechatbot_secure_password
POSTGRES_PORT=5432

# Optional: DeepSeek API key (can also be set in UI)
DEEPSEEK_API_KEY=""

# Application URL
NEXT_PUBLIC_API_URL="http://localhost:3000"

# Optional: Nginx Configuration (if using nginx profile)
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
